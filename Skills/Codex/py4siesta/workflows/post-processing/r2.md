# Post-processing — MCP revision 2

- Workflow ID: post-processing
- Revision: r2
- Status: draft
- Parent/source: [CLI recipe revision 1](../../references/workflows/post-processing.md)
- Source recipe SHA-256 before archival annotation: 6c16ebb3c9646c46d9826b8bfdd60680debd9e35af1e74eb11acb26b71cb28c0
- Tool contract review: names and example parameters checked against this checkout's
  `py4siesta_mcp.catalog.Catalog` and CLI parser; recheck the connected server.
- Execution validation: unverified; no MCP calculation or scheduler trial performed
  for this migration. Never auto-select as a validated workflow.
- Change: express existing operations as MCP calls while retaining recipe constraints.

## Required MCP tools

- `py4siesta_tool_band` → `py4siesta-tool.band`
- `py4siesta_tool_pdos` → `py4siesta-tool.pdos`
- `py4siesta_tool_pldos` → `py4siesta-tool.pldos`

Calls below use the client-exposed names corresponding to these server tool names.
Replace the example absolute workdir and scientific inputs; read the complete
procedure before execution. Follow [MCP prerequisites](../../references/mcp.md).
File/figure inspection can use host tools; no CLI fallback.

## Procedure, inputs, outputs, and validation

Menus: `11`, `12`, `13`. Read [mcp.md](../../references/mcp.md) before execution. No origin directory needed.
Select only the requested band, PDOS, and/or PLDOS branches below.
Use explicit file paths when multiple labels exist. Confirm the energy reference,
window and selected orbitals with the task; do not reuse defaults across systems blindly.

## Band structure (11)

```text
py4siesta_tool_band {"parameters": {"bands_path": "Solid.bands", "emin": -2.0, "emax": 4.0}, "workdir": "/absolute/calculation/directory"}
```

Requires a readable .bands file. Writes `band.png`, `specialk.csv`, `kpath.csv`,
`band.csv` in the execution cwd. Check finite energies, path/tick consistency, energy
reference and plotted window; a reported gap is limited to the sampled band path.

## PDOS (12)

```text
py4siesta_tool_pdos {"parameters": {"pdos_path": "MgO.PDOS", "orbital": [["Mg_0", "O_0"]], "emin": -4.0, "emax": 12.0}, "workdir": "/absolute/calculation/directory"}
```

Requires matching .EIG and .DOS beside .PDOS and configured fmpdos. Optional matching
.bands supplies the valence reference; otherwise the implementation estimates it from
.EIG. Read the returned reference/VBM/spin metadata. Selections use
`atom_or_species[_n[_l[_m]]]`; use a nested orbital array as shown in the MCP call.
Verify selections against the actual species/atom indices.

Writes `PDOS.csv`, `pdos.png` and selection files beside the input .PDOS. Existing
same-named selection files are deleted before extraction. Verify requested columns,
finite/nonempty data, energy grid alignment and spin labels. Preserve existing products
if replacement was not requested.

## PLDOS (13)

```text
py4siesta_tool_pldos {"parameters": {"pdos_path": "Device.PDOS", "emin": -4.0, "emax": 2.0}, "workdir": "/absolute/calculation/directory"}
```

Requires matching .xyz and .EIG and the configured NanoCore PDOS utility used by
`siesta.get_pdos`. Optional parameter keys include zmin, zmax, broad and npoints; inspect
the connected tool schema. Writes `pldos.png`, `pldos_z.csv`, `pldos_energy.csv`, `pldos.csv` beside
.PDOS. Verify z groups, energy reference (Fermi), dimensions and nonempty density.
Current implementation uses the spin-up PDOS channel and exports log density; do not
describe it as a spin-summed linear DOS without checking/changing the requested scope.

For all three, verify files and inspect the figure. Missing utilities, parse failures,
empty windows or inconsistent companion files are failures to diagnose, not reasons
to fabricate outputs. Reprocessing can overwrite products, so record their paths.
