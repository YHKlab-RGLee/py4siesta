# Geometry optimization — MCP revision 2

- Workflow ID: geometry-optimization
- Revision: r2
- Status: draft
- Parent/source: [CLI recipe revision 1](../../references/workflows/geometry-optimization.md)
- Source recipe SHA-256 before archival annotation: 7a3ebe8df577a07cd97f006baf4322c3f6ce0fc311ba3ad3765930c8fd3ebe9f
- Tool contract review: names and example parameters checked against this checkout's
  `py4siesta_mcp.catalog.Catalog` and CLI parser; recheck the connected server.
- Execution validation: unverified; no MCP calculation or scheduler trial performed
  for this migration. Never auto-select as a validated workflow.
- Change: express existing operations as MCP calls while retaining recipe constraints.

## Required MCP tools

- `py4siesta_tool_distance_current` → `py4siesta-tool.distance-current`
- `py4siesta_tool_distance_scan` → `py4siesta-tool.distance-scan`
- `py4siesta_tool_eos_bulk` → `py4siesta-tool.eos-bulk`
- `py4siesta_tool_eos_slab` → `py4siesta-tool.eos-slab`
- `py4siesta_tool_eos_sliding` → `py4siesta-tool.eos-sliding`
- `py4siesta_tool_fit_structure` → `py4siesta-tool.fit-structure`

Calls below use the client-exposed names corresponding to these server tool names.
Replace the example absolute workdir and scientific inputs; read the complete
procedure before execution. Follow [MCP prerequisites](../../references/mcp.md).
Submission branches additionally require the tools and checks in the linked
submission workflow. File/figure inspection can use host tools; no CLI fallback.

## Procedure, inputs, outputs, and validation

Menus: `4–8`, optionally `10`. Read [mcp.md](../../references/mcp.md) before execution.

Clarify whether the goal is preparing a scan, running configured SIESTA relaxations,
or fitting completed energies. These menus generate scans and fits; actual ionic
relaxation settings come from origin/input/RUN.fdf and the user's job scripts.

## Select and prepare

| Menu | MCP tool and arguments | Output directory / interpretation |
| --- | --- | --- |
| 4 | `py4siesta_tool_eos_bulk {"parameters": {"scale_mask": [1, 1, 1]}, "workdir": "/absolute/calculation/directory"}` | `02.volume_eos`; 11 scale ratios, 0.99–1.01 |
| 5 | `py4siesta_tool_eos_slab {"parameters": {"scale_mask": [1, 1, 0]}, "workdir": "/absolute/calculation/directory"}` | `02.slab_eos`; 11 scale ratios, 0.98–1.02 |
| 6 | `py4siesta_tool_eos_sliding {"parameters": {"selection": "20-30", "mode": "fractional", "vector": [[0.25, 0.5]]}, "workdir": "/absolute/calculation/directory"}` | `02.sliding`; selected-atom lateral scan |
| 7 | `py4siesta_tool_distance_current {"parameters": {"selection": "20-30"}, "workdir": "/absolute/calculation/directory"}` | Current minimum absolute z separation |
| 7 | `py4siesta_tool_distance_scan {"parameters": {"selection": "20-30", "start": 2.8, "end": 3.2, "points": 5}, "workdir": "/absolute/calculation/directory"}` | `02.distance`; requested z separations |

Call the named MCP tools with the illustrated argument objects; replace example
values and workdir with the task inputs. Selection is 1-based and must leave both moving and fixed atoms. Inspect atom
identities before selecting. Fractional sliding uses in-plane cell vectors; absolute
sliding uses Cartesian x/y displacements. Distance is minimum absolute z separation,
not nearest 3D distance. Confirm that this definition matches the user's system.

Scale masks act on Cartesian x/y/z components of cell vectors and atomic positions,
not simply named lattice-vector lengths. Inspect nonorthogonal cells carefully.
The current EOS CLI exposes masks but not custom ratio ranges; do not invent flags.
If another range is required, report that interface limitation before changing scope.

Validate generated case counts, STRUCT/KPT files, copied calculation settings, and
fixed/moving coordinates. Preserve existing output directories before regeneration.
For requested execution follow [submission.md](../submission/r2.md), menu 10.

## Analyze completed cases (8)

- Bulk (8 → 1): `py4siesta_tool_fit_structure {"parameters": {"mode": "Murnaghan"}, "workdir": "/absolute/calculation/directory"}`
- Slab (8 → 2): `py4siesta_tool_fit_structure {"parameters": {"mode": "Polynomial"}, "workdir": "/absolute/calculation/directory"}`
- Distance (8 → 3): `py4siesta_tool_fit_structure {"parameters": {"mode": "Distance", "selection": "20-30"}, "workdir": "/absolute/calculation/directory"}`

Check every intended case's `OUT/stdout.txt` for completion, total energy, cell volume,
and applicable cell-vector modules. The implementation computes a fourth-degree fit
as well as a volume fit; require enough distinct valid points (at least five) and
check conditioning. Constant/degenerate fit variables may fail even with enough files;
do not claim success or change the scientific setup just to suppress warnings.

The fitter recreates `<scan>/optimized_structure`, writes `eos_fitting.png`, and
places the fitted structure in `optimized_structure/input/STRUCT.fdf`. Murnaghan also
writes `eos_fitting_parameters.dat`. Verify finite parameters, minimum inside sampled
range, fit quality, and output cell/coordinates. A fitted structure is not a subsequent
SIESTA relaxation. There is no menu-8 sliding-fit mode; do not route menu 6 to a slab fit.
