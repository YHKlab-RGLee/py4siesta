# K-point convergence — MCP revision 2

- Workflow ID: kpoint-convergence
- Revision: r2
- Status: draft
- Parent/source: [CLI recipe revision 1](../../references/workflows/kpoint-convergence.md)
- Source recipe SHA-256 before archival annotation: a7102df81807f0e244fb466731b5eed1ca9f597fb64f1577548b8b2637266a19
- Tool contract review: names and example parameters checked against this checkout's
  `py4siesta_mcp.catalog.Catalog` and CLI parser; recheck the connected server.
- Execution validation: unverified; no MCP calculation or scheduler trial performed
  for this migration. Never auto-select as a validated workflow.
- Change: express existing operations as MCP calls while retaining recipe constraints.

## Required MCP tools

- `py4siesta_tool_kpoint_analysis` → `py4siesta-tool.kpoint-analysis`
- `py4siesta_tool_kpoint_bulk` → `py4siesta-tool.kpoint-bulk`
- `py4siesta_tool_kpoint_slab` → `py4siesta-tool.kpoint-slab`

Calls below use the client-exposed names corresponding to these server tool names.
Replace the example absolute workdir and scientific inputs; read the complete
procedure before execution. Follow [MCP prerequisites](../../references/mcp.md).
Submission branches additionally require the tools and checks in the linked
submission workflow. File/figure inspection can use host tools; no CLI fallback.

## Procedure, inputs, outputs, and validation

Menus: `1`, `2`, `3`, optionally `9`. Read [mcp.md](../../references/mcp.md) before execution.

Select preparation-only, preparation-and-submission, analysis-only, or complete
calculation from the request. For analysis-only, inspect existing cases and start
at step 4; never regenerate them. For a complete calculation, wait for verified
completion before step 4. Missing monitoring capability leaves the task waiting.

1. Confirm bulk/slab geometry, user-selected positive integer grid samples, origin
   inputs, and tolerance in eV per calculation cell (not per atom).
2. Prepare in a directory without existing results to preserve:

   ```text
   py4siesta_tool_kpoint_bulk {"parameters": {"kpoints": [2, 4, 6]}, "workdir": "/absolute/calculation/directory"}
   # Or for a slab:
   py4siesta_tool_kpoint_slab {"parameters": {"kpoints": [2, 4, 6]}, "workdir": "/absolute/calculation/directory"}
   ```

   These are examples, not recommended convergence settings. Both recreate
   `01.kpoint_sampling`; inspect each case's `input/KPT.fdf` for k×k×k or k×k×1.
3. If calculation execution is requested, follow [submission.md](../submission/r2.md)
   for menu 9. Otherwise finish with prepared case paths.
4. Before analysis, compare expected cases against `OUT/stdout.txt`, checking real
   calculation termination and convergence. The collector skips missing energies;
   a subset must not be reported as a complete sweep.
5. Call `py4siesta_tool_kpoint_analysis {"parameters": {"tolerance": 0.01}, "workdir": "/absolute/calculation/directory"}` with the agreed threshold.
   Verify `01.kpoint_sampling/kpoint_convergence.dat` and `kpoint_convergence.png`,
   numeric energies, included case count, and the returned `converged_k`.

The implemented criterion finds the first index after which every adjacent sampled
total-energy difference is <= tolerance. A null converged_k means no convergence was
identified; successful analysis alone does not establish convergence. Report sampling
limits. Any denser sweep must preserve completed results and maintain consistent
structure, basis, pseudopotentials, and other calculation settings.
