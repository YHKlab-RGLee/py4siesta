# Runtime contract

The skill now requires a user-prepared py4siesta MCP connection for calculation
execution. Read [mcp.md](mcp.md) for environment prerequisites, discovery, object
handling, and file/result constraints. Direct CLI/Python execution and automatic
environment setup are no longer execution paths for this skill.

Historical recipes under `references/workflows/` preserve earlier guidance only;
current MCP workflow candidates are under `workflows/` and are not validated merely
by being migrated.
